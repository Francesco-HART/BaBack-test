# Baback Interview Frontend

Le code à modifier se situe essentiellement dans `components/basket`.  
Le dossier `data` contient des données de demo et des exemples de types

### What's in

- React + Vite
- TypeScript
- UI: Shadcn
- Some useful libs

### Run

```
npm i
npm run dev
```
